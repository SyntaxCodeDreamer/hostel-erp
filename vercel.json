{
  "framework": "vite",
  "buildCommand": "npm --prefix frontend run build || npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
